experiment:
  name: "smoke_test"
  seed: 42

dataset:
  name: "aa_lcr"
  samples: 2  # Small sample
  domains: ["Legal"]
  dataset_folder: "tests/fixtures/aa_lcr_smoke/AA-LCR_extracted-text/lcr"

systems:
  - name: "stub_system"
    type: "stub"
    command: []

metrics:
  - exact_match
  - soft_recall
  - soft_f1
  - verbosity
  - latency_s
  - refusal_rate
  - error_rate
  - answer_length
  - citation_presence
  - confidence_score
  - answer_completeness
  - reasoning_depth
  - response_consistency

  - rouge_score_f1
  - rouge_score_recall
  - bert_score_f1

metric_args:
  bert_score_f1:
    model_type: "distilbert-base-uncased"

judges:
  strategy: "fixed"
  fixed:
    provider: "openai"
    model: "gpt-4o-mini-2024-07-18"
  templates:
    correctness: "default"
    hallucination: "default"

statistics:
  bootstrap_resamples: 100
  alpha: 0.05
  primary_test: "wilcoxon_one_tailed"
  secondary_test: "wilcoxon_two_tailed"

output:
  runs_dir: "./runs"
  log_level: "INFO"
